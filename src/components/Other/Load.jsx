import React from 'react';
import "../styles/load.scss";

const Load = () => {
      return (
            <div className='load'>
                  <h1>LOAD....</h1>
            </div>
      );
};

export default Load;